# 核心管线
pdfplumber>=0.11
Pillow>=10.0
openpyxl>=3.1
# 数据层
SQLAlchemy>=2.0
# 服务层
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
jinja2>=3.1          # 数据后台模板
pydantic>=2.0        # 接口模型（fastapi 已带，显式声明以免版本漂移）
httpx>=0.27          # 调用 LLM 与飞书 API
# IM 通道
lark-oapi>=1.7       # 飞书长连接与消息收发
# 文字识别（容器内主力，macOS 上可由 Vision 替代）
rapidocr-onnxruntime>=1.3
opencv-python-headless>=4.9
